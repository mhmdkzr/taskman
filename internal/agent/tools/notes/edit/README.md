# notes_edit

Replaces the body of an existing note in the current session. It never creates a note.

```json
{"name":"release-plan","body":"Ship on Friday."}
```

Returns the updated note id, name, body, and creation timestamp.
