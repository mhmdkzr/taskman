# notes_list

Lists notes owned by the current session, newest first. `limit` defaults to 20 and is bounded to 100.

```json
{"limit":10}
```

Returns note ids, names, bodies, and creation timestamps.
