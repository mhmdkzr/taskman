# Read File Tool

`read_file` reads a text file with optional 1-indexed `offset` and `limit` line bounds. When given a directory, it returns a sorted entry list and omits image and PDF files.

Example input:

```json
{"path":"internal/agent/tools"}
```
