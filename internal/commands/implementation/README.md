# implementation

The `implementation` grouping command. It has no behavior of its own - it only mounts
`taskman implementation review`, the implementation-stage review tree. The `implemented` report
itself is a separate top-level command, not part of this group.
