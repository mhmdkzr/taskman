# agent

The `specification review agent` grouping command. It has no behavior of its own - it mounts
`approved` and `rejected`, which report an independent agent reviewer's decision on the
specification.
