// Package process bootstraps and starts the application: loads config, connects
// SQLite, initializes logger, runs migrations, and starts
// the HTTP server with its middleware chain.
package process
