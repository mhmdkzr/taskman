# `natsembed`

Import path: `github.com/mhmdkzr/app/pkg/natsembed`

Starts an in-process embedded NATS server with JetStream via `Connect()`, returning a connected `*nats.Conn` and `jetstream.JetStream` context for tests.
